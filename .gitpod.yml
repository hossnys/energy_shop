image:
  file: .gitpod.Dockerfile
tasks:
    - name: shop
      command: |
        sudo -s && sudo poetry run threebot start --no-cert --background
ports:
  - port: 80
    onOpen: open-browser
  - port: 443
    onOpen: open-browser
